---
title: SplitButton
description: A split action built from ButtonGroup with a primary action and a separate toggle trigger.
---

import {
  SplitButtonDefaultPreview,
  SplitButtonPreview,
} from '~/components/previews/SplitButtonPreview';

## Preview

<SplitButtonPreview />

## Usage

```tsx
import SplitButton from '@/components/ui/SplitButton';
import { useState } from 'react';

function SaveButton() {
  const [open, setOpen] = useState(false);

  return (
    <SplitButton
      theme="primary"
      open={open}
      onClick={() => handleSave()}
      splitOnClick={() => setOpen((v) => !v)}
    >
      Save
    </SplitButton>
  );
}
```

## Default Theme

<SplitButtonDefaultPreview />

```tsx
<SplitButton
  theme="default"
  open={open}
  onClick={handleAction}
  splitOnClick={toggleOpen}
>
  Action
</SplitButton>
```

## Props

| Prop                    | Type                      | Default     | Description                                                          |
| ----------------------- | ------------------------- | ----------- | -------------------------------------------------------------------- |
| `open`                  | `boolean`                 | —           | **Required.** Controls whether the split arrow appears in open state |
| `theme`                 | `'primary' \| 'default'`  | `'default'` | Color theme                                                          |
| `size`                  | `'medium' \| 'large'`     | `'medium'`  | Button size                                                          |
| `onClick`               | `(e: MouseEvent) => void` | —           | Handler for the main button area                                     |
| `splitOnClick`          | `(e: MouseEvent) => void` | —           | Handler for the arrow trigger                                        |
| `icon`                  | `ReactNode`               | —           | Icon rendered in the main button area                                |
| `focusableWhenDisabled` | `boolean`                 | `false`     | Keeps keyboard focus on the button when disabled styling is applied  |
| `disabled`              | `boolean`                 | `false`     | Disables the entire button                                           |

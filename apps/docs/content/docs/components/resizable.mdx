---
title: Resizable
description: Accessible resizable panels backed by react-resizable-panels.
---

import { ComponentPreview } from '~/components/preview/ComponentPreview';
import {
  ResizableHandle,
  ResizablePanel,
  ResizablePanelGroup,
} from '~/components/vital-components';

## Preview

<ComponentPreview centered={false}>
  <ResizablePanelGroup
    orientation="horizontal"
    className="min-h-[200px] max-w-md rounded-md border border-grayscale-300"
  >
    <ResizablePanel defaultSize="50%">
      <div className="flex h-full items-center justify-center p-6 text-sm font-medium text-grayscale-800">
        One
      </div>
    </ResizablePanel>
    <ResizableHandle />
    <ResizablePanel defaultSize="50%">
      <ResizablePanelGroup orientation="vertical">
        <ResizablePanel defaultSize="35%">
          <div className="flex h-full items-center justify-center p-6 text-sm font-medium text-grayscale-800">
            Two
          </div>
        </ResizablePanel>
        <ResizableHandle />
        <ResizablePanel defaultSize="65%">
          <div className="flex h-full items-center justify-center p-6 text-sm font-medium text-grayscale-800">
            Three
          </div>
        </ResizablePanel>
      </ResizablePanelGroup>
    </ResizablePanel>
  </ResizablePanelGroup>
</ComponentPreview>

## Usage

```tsx
import {
  ResizableHandle,
  ResizablePanel,
  ResizablePanelGroup,
} from '@/components/ui/Resizable';

<ResizablePanelGroup orientation="horizontal">
  <ResizablePanel defaultSize="50%">One</ResizablePanel>
  <ResizableHandle />
  <ResizablePanel defaultSize="50%">Two</ResizablePanel>
</ResizablePanelGroup>;
```

`Resizable` wraps
[`react-resizable-panels`](https://github.com/bvaughn/react-resizable-panels).
Compose `ResizablePanelGroup` with `ResizablePanel` children separated
by `ResizableHandle`. Nest groups to build mixed horizontal/vertical
layouts.

## Vertical

<ComponentPreview centered={false}>
  <ResizablePanelGroup
    orientation="vertical"
    className="min-h-[240px] max-w-md rounded-md border border-grayscale-300"
  >
    <ResizablePanel defaultSize="30%">
      <div className="flex h-full items-center justify-center p-6 text-sm font-medium text-grayscale-800">
        Header
      </div>
    </ResizablePanel>
    <ResizableHandle />
    <ResizablePanel defaultSize="70%">
      <div className="flex h-full items-center justify-center p-6 text-sm font-medium text-grayscale-800">
        Content
      </div>
    </ResizablePanel>
  </ResizablePanelGroup>
</ComponentPreview>

```tsx
<ResizablePanelGroup orientation="vertical">
  <ResizablePanel defaultSize="30%">Header</ResizablePanel>
  <ResizableHandle />
  <ResizablePanel defaultSize="70%">Content</ResizablePanel>
</ResizablePanelGroup>
```

## With Handle

Pass `withHandle` to `ResizableHandle` to render a visible drag
indicator. Use a `minSize` on a panel to prevent it from collapsing.

<ComponentPreview centered={false}>
  <ResizablePanelGroup
    orientation="horizontal"
    className="min-h-[200px] max-w-md rounded-md border border-grayscale-300"
  >
    <ResizablePanel defaultSize="30%" minSize="20%">
      <div className="flex h-full items-center justify-center p-6 text-sm font-medium text-grayscale-800">
        Sidebar
      </div>
    </ResizablePanel>
    <ResizableHandle withHandle />
    <ResizablePanel defaultSize="70%">
      <div className="flex h-full items-center justify-center p-6 text-sm font-medium text-grayscale-800">
        Content
      </div>
    </ResizablePanel>
  </ResizablePanelGroup>
</ComponentPreview>

```tsx
<ResizablePanelGroup orientation="horizontal">
  <ResizablePanel defaultSize="30%" minSize="20%">
    Sidebar
  </ResizablePanel>
  <ResizableHandle withHandle />
  <ResizablePanel defaultSize="70%">Content</ResizablePanel>
</ResizablePanelGroup>
```

## Parts

| Part                  | Description                                                  |
| --------------------- | ------------------------------------------------------------ |
| `ResizablePanelGroup` | Layout container; sets `horizontal` or `vertical` direction  |
| `ResizablePanel`      | Resizable region inside a group                              |
| `ResizableHandle`     | Drag handle separator between two panels                     |

## ResizablePanelGroup Props

Accepts every prop from `react-resizable-panels`'s
[`PanelGroup`](https://github.com/bvaughn/react-resizable-panels#panelgroup).

| Prop          | Type                          | Default       | Description                              |
| ------------- | ----------------------------- | ------------- | ---------------------------------------- |
| `orientation` | `'horizontal' \| 'vertical'`  | `'horizontal'`| Layout direction                         |
| `autoSaveId`  | `string`                      | —             | Persist sizes in `localStorage`          |
| `className`   | `string`                      | —             | Additional CSS classes                   |

## ResizablePanel Props

Accepts every prop from `react-resizable-panels`'s
[`Panel`](https://github.com/bvaughn/react-resizable-panels#panel).

| Prop          | Type                | Default | Description                              |
| ------------- | ------------------- | ------- | ---------------------------------------- |
| `defaultSize` | `number \| string`  | —       | Initial size as percentage               |
| `minSize`     | `number \| string`  | —       | Minimum size before collapse             |
| `maxSize`     | `number \| string`  | —       | Maximum size                             |
| `collapsible` | `boolean`           | `false` | Allow collapsing past `collapsedSize`    |
| `id`          | `string`            | —       | Required if `autoSaveId` is set on group |

## ResizableHandle Props

| Prop         | Type      | Default | Description                                  |
| ------------ | --------- | ------- | -------------------------------------------- |
| `withHandle` | `boolean` | `false` | Renders a visible grip indicator             |
| `disabled`   | `boolean` | `false` | Disables dragging                            |
| `className`  | `string`  | —       | Additional CSS classes                       |

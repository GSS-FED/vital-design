---
title: Button
description: Trigger actions with default, text, and ghost variants, multiple themes, and icon support.
---

import { SearchIcon } from '@/icons/SearchIcon';
import { ComponentPreview } from '~/components/preview/ComponentPreview';
import { Button, Spinner } from '~/components/vital-components';

## Preview

<ComponentPreview>
  <Button theme="primary" size="lg">
    Primary
  </Button>
  <Button theme="default" size="lg">
    Default
  </Button>
  <Button theme="success" size="lg">
    Success
  </Button>
  <Button theme="alarm" size="lg">
    Alarm
  </Button>
</ComponentPreview>

## Usage

```tsx
import { Button } from '@/components/ui/Button';

<Button theme="primary" size="lg">
  Click me
</Button>;
```

## Variants

### Default

The `default` variant renders a solid background button. It is the
default rendering style.

<ComponentPreview>
  <Button variant="default" theme="primary">
    Primary
  </Button>
  <Button variant="default" theme="default">
    Default
  </Button>
  <Button variant="default" theme="success">
    Success
  </Button>
  <Button variant="default" theme="info">
    Info
  </Button>
  <Button variant="default" theme="warning">
    Warning
  </Button>
  <Button variant="default" theme="alarm">
    Alarm
  </Button>
  <Button variant="default" theme="dangerous">
    Dangerous
  </Button>
</ComponentPreview>

```tsx
<Button variant="default" theme="primary">Primary</Button>
<Button variant="default" theme="default">Default</Button>
<Button variant="default" theme="success">Success</Button>
<Button variant="default" theme="info">Info</Button>
<Button variant="default" theme="warning">Warning</Button>
<Button variant="default" theme="alarm">Alarm</Button>
<Button variant="default" theme="dangerous">Dangerous</Button>
```

### Text

The `text` variant renders a transparent background button. Available for all themes except `dangerous`.

<ComponentPreview>
  <Button variant="text" theme="primary">
    Primary
  </Button>
  <Button variant="text" theme="default">
    Default
  </Button>
  <Button variant="text" theme="success">
    Success
  </Button>
  <Button variant="text" theme="info">
    Info
  </Button>
  <Button variant="text" theme="warning">
    Warning
  </Button>
  <Button variant="text" theme="alarm">
    Alarm
  </Button>
</ComponentPreview>

```tsx
<Button variant="text" theme="primary">Primary</Button>
<Button variant="text" theme="default">Default</Button>
```

### Ghost

The `ghost` variant renders a transparent button that surfaces a
background on hover. Use it inside popovers, toolbars, and split-button
menus where the surrounding chrome provides the affordance.

<ComponentPreview>
  <Button variant="ghost" theme="primary">
    Primary
  </Button>
  <Button variant="ghost" theme="default">
    Default
  </Button>
</ComponentPreview>

```tsx
<Button variant="ghost" theme="default">
  Default
</Button>
```

### Sizes

<ComponentPreview>
  <Button theme="primary" size="md">
    Medium
  </Button>
  <Button theme="primary" size="lg">
    Large
  </Button>
  <Button theme="primary" size="icon-md" aria-label="Search">
    <SearchIcon data-icon="inline-start" />
  </Button>
</ComponentPreview>

```tsx
<Button size="md">Medium</Button>
<Button size="lg">Large</Button>
<Button size="icon-md" aria-label="Search">
  <SearchIcon data-icon="inline-start" />
</Button>
```

### Icons

Use `data-icon="inline-start"` or `data-icon="inline-end"` on child icons. For icon-only buttons, use `size="icon-md"` or `size="icon-lg"`.
Install or import the icon or spinner separately from the button.

<ComponentPreview>
  <Button theme="primary" size="lg">
    <SearchIcon data-icon="inline-start" />
    Search
  </Button>
  <Button theme="default" size="lg">
    Search
    <SearchIcon data-icon="inline-end" />
  </Button>
</ComponentPreview>

```tsx
<Button theme="primary">
  <SearchIcon data-icon="inline-start" />
  Search
</Button>
<Button theme="default">
  Search
  <SearchIcon data-icon="inline-end" />
</Button>
```

### Loading State

Render a spinner as a child icon and disable the button while work is in flight.

<ComponentPreview>
  <Button theme="primary" size="lg" disabled>
    <Spinner data-icon="inline-start" />
    Saving
  </Button>
  <Button theme="default" size="lg" disabled>
    Loading
    <Spinner data-icon="inline-end" />
  </Button>
</ComponentPreview>

```tsx
<Button theme="primary" disabled>
  <Spinner data-icon="inline-start" />
  Saving
</Button>
```

### Disabled

<ComponentPreview>
  <Button theme="primary" size="lg" disabled>
    Disabled
  </Button>
  <Button variant="text" theme="primary" size="lg" disabled>
    Disabled
  </Button>
</ComponentPreview>

```tsx
<Button theme="primary" disabled>
  Disabled
</Button>
```

## Props

### Button

| Prop                    | Type                                                                                   | Default                                       | Description                                                      |
| ----------------------- | -------------------------------------------------------------------------------------- | --------------------------------------------- | ---------------------------------------------------------------- |
| `variant`               | `'default' \| 'text' \| 'ghost'`                                                       | `'default'`                                   | Visual variant                                                   |
| `theme`                 | `'primary' \| 'default' \| 'success' \| 'info' \| 'warning' \| 'alarm' \| 'dangerous'` | `'primary'` for default, `'default'` for text | Color theme (`'dangerous'` only available on `default`)          |
| `size`                  | `'md' \| 'lg' \| 'icon-md' \| 'icon-lg'`                                               | `'md'`                                        | Button size                                                      |
| `focusableWhenDisabled` | `boolean`                                                                              | `false`                                       | Keeps the button focusable even when disabled styling is applied |
| `disabled`              | `boolean`                                                                              | `false`                                       | Native disabled attribute                                        |
| `className`             | `string`                                                                               | —                                             | Classes for the `<button>` itself                                |
| `contentClassName`      | `string`                                                                               | —                                             | Classes for the inner content wrapper (see below)                |
| `style`                 | `CSSProperties`                                                                        | —                                             | Inline styles                                                    |

### Laying out the content

`Button` wraps its children in a `span[data-slot="button-content"]` that centres
the icon and label and keeps them on one line. Since that span is the button's
only child, flex utilities written on the button itself have nothing to
distribute — use `contentClassName` to reach the wrapper instead of selecting
into it with `[&>span]:*`.

```tsx
// Icon on the left, count pushed to the right, label allowed to wrap
<Button contentClassName="w-full justify-between whitespace-normal">
  <FlagIcon data-icon="inline-start" />
  Reported issues
  <span>12</span>
</Button>
```

Colour, size, radius, and hover states stay owned by the theme — passing those
through `className` will not override the variant. The button also sets
`overflow-hidden` for its hover overlay, so a child positioned outside the
button box (a notification badge at `-top-1 -right-1`) needs
`className="overflow-visible"` on the button.

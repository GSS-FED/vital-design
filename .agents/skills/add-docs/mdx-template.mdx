---
title: ComponentName
description: One-line description of what the component does and when to use it.
---

import { ComponentNamePreview } from '~/components/previews/ComponentNamePreview';

## Preview

<ComponentNamePreview />

## Usage

```tsx
import ComponentName from '@/components/component-name/ComponentName';

function Example() {
  return <ComponentName />;
}
```

## Variants

<!-- Optional section: add a second preview + description if the component has distinct variants -->

<!-- <ComponentNameVariantPreview /> -->

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `prop` | `type` | — | Description |
| `onChange` | `(value: type) => void` | — | Called when the value changes |
| `disabled` | `boolean` | `false` | Disables the component |
| `className` | `string` | — | Additional CSS classes |
